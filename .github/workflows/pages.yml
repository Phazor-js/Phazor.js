name: 🚨 Stop Deploy 🚨

# Just to know
# We use deploy.yml file to make deploy to gh pages

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🚨 Use deploy.yml File 🚨
        run: echo We use deploy.yml File to run pages
   
     
